## Before Executing the file, install these dependencies:-
pip install python-dotenv langchain langchain-community langchain-openai faiss-cpu pymupdf tiktoken

and add an .env file where you can keep the endpoints and keys

AZURE_OPENAI_API_KEY= **************

AZURE_OPENAI_ENDPOINT = **************

AZURE_OPENAI_API_VERSION = **************
